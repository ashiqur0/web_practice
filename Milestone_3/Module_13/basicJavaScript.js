/**
 * variable: naming convention, var, const
 * data type: Number, string, boolean
 * concatination: string concatination
 * type conversion: parseInt(), parseFloat(), Number(), ().toString
 * fixed size: toFixed()
 * operator: 
 * operation: 
 */

var name = "Ashiqur";
var roll = 9;

console.log(name);